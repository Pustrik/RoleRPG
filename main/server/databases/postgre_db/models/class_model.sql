CREATE TABLE classes(
    id INTEGER,
    name VARCHAR(255),
    health INTEGER,
    damage INTEGER,
    attack_type INTEGER,
    ability VARCHAR(225),
    created_at VARCHAR(225),
    updated_at VARCHAR(225)
);